name: Lint

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: ArkinSolomon/xpkg-web-apps/.github/workflows/setup.yml@master
      - run: npx nx affected -t lint --parallel=3
      - uses: actions-x/commit@v6
        with:
          message: "Automatically lint"
          token: ${{ secrets.GITHUB_TOKEN }}

