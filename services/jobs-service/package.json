{
  "name": "@xpkg/jobs-service",
  "version": "0.1.0",
  "description": "Manages workers to watch out for crashes and/or errors.",
  "main": "dist/index.js",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "npx tsc --build --clean ",
    "install-deps": "npm install",
    "build": "npx tsc --build",
    "start": "node --trace-warnings --enable-source-maps -r dotenv/config .",
    "lint": "npx eslint --fix",
    "check-lint": "npx eslint"
  },
  "author": "Arkin Solomon",
  "license": "Apache-2.0",
  "dependencies": {
    "@xpkg/backend-util": "^0.1.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "hasha": "^5.2.2",
    "mongoose": "^8.0.0",
    "pino": "^8.16.1",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.20"
  }
}
